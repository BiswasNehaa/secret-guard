# Fixture intent: this directory intentionally has no .git and no
# .gitignore of its own, proving the scanner works standalone without
# either (Scanner only ever looks for a .gitignore at its own root, never
# a parent's, so this is a faithful "no git context" case).
AWS_KEY = "AKIAIOSFODNN7EXAMPLE"
